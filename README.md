# 戦国戦況図タイムラインエディタ

Sengoku Battle Map Timeline Editor は、戦国時代の合戦・城攻め・勢力移動を地図上に配置し、日付ごとの変化として編集・再生するためのWebアプリです。YouTube歴史解説動画の戦況図素材作成を想定したMVPです。

## 技術スタック

- React
- TypeScript
- Vite
- React Konva / Konva
- Zustand
- 通常CSS
- JSONベース保存

外部APIやサーバーは使っていません。

## 起動方法

```bash
npm install
npm run dev
```

表示されたローカルURLをブラウザで開きます。

## ビルド方法

```bash
npm run build
```

ビルド成果物は `dist/` に出力されます。

## 基本操作

- サンプル選択: 上部ツールバーの「サンプル」から読み込み
- 地図画像読み込み: 「地図画像」ボタンから画像ファイルを選択
- JSON保存: 「JSON保存」または `Ctrl + S`
- JSON読み込み: 「JSON読込」から保存済みJSONを選択
- PNG出力: 「PNG」ボタン
- Undo / Redo: ボタンまたは `Ctrl + Z` / `Ctrl + Y`
- コマ移動: 軍勢コマをドラッグすると現在日付のキーフレームを作成・更新
- 日付移動: 下部タイムライン、または `←` / `→`
- 再生: 下部タイムラインの「再生」
- 線・矢印描画: 描画モードでキャンバスをクリックして点を追加、`Enter` で確定、`Esc` でキャンセル
- パン: `Space + ドラッグ`
- ズーム: マウスホイール
- 削除: 選択中に `Delete`

## サンプルデータについて

同梱サンプルは以下の2つです。

- 長篠・設楽原風デモ
- 三木城包囲戦風デモ

どちらも表示確認用の仮データです。史実確定データではありません。`description`、`memo`、`sourceNote` にもその旨を明記しています。

## JSON保存仕様

プロジェクトは `ProjectData` としてJSON保存されます。主な構成は以下です。

- `timeline`: 日付フレーム、現在日付、補間モード
- `map`: 地図画像Data URL、出力サイズ
- `factions`: 陣営
- `units`: 軍勢コマと日付キーフレーム
- `sites`: 城・砦・寺・港などの拠点
- `lines`: 包囲線・防御線など
- `arrows`: 進軍・撤退・援軍ルート
- `events`: イベントマーカー
- `labels`: 注釈ラベル

Web版では地図画像をData URLとしてJSON内に保持できます。

## 相対座標

軍勢、拠点、線、矢印、イベント、ラベルの座標は、地図画像上の相対座標で保存します。

```json
{ "x": 0.5, "y": 0.5 }
```

これは地図中央を意味します。絶対ピクセル座標は保存せず、キャンバス表示時だけ内部基準サイズに変換します。

## 和暦変換

正確な旧暦・和暦変換は未実装です。`time` は内部ソート用文字列、`displayDate` は表示用文字列として扱います。

## Tauri化の状況

現時点ではWeb版MVPとして実装しています。Tauri v2は未導入です。ファイル入出力は `src/utils/fileIO.ts` に分離しているため、後からTauriのファイル保存・読み込みAPIへ置き換えやすい構成にしています。

## 今後の拡張案

- 家紋画像アイコン
- 複数地図切り替え
- 城攻め専用モード
- 兵糧ゲージ
- 兵数推移グラフ
- 史料出典管理
- 異説ルートの切り替え
- 連番PNG出力
- MP4出力
- 1920x1080固定出力
- レイヤー管理
- 地理座標・緯度経度対応
- GeoJSON対応
- YouTube用テロップ出力
